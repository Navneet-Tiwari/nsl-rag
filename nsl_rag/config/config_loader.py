"""
config_loader.py
----------------
ConfigLoader class. Loads, validates, and exposes settings.yaml + .env. Single access point for all config across the system. Fails fast on missing required config.
"""
